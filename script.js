const menuBtn=document.getElementById("menuBtn"),nav=document.getElementById("navLinks");menuBtn.addEventListener("click",()=>nav.classList.toggle("open"));document.querySelectorAll("nav a").forEach(a=>a.addEventListener("click",()=>nav.classList.remove("open")));const lb=document.getElementById("lightbox"),lbImg=document.getElementById("lightboxImg");document.querySelectorAll("img").forEach(img=>{if(img.closest(".lightbox")||img.closest(".hero-photo"))return;img.addEventListener("click",()=>{lbImg.src=img.src;lb.classList.add("active")})});document.getElementById("closeLightbox").addEventListener("click",()=>lb.classList.remove("active"));lb.addEventListener("click",e=>{if(e.target===lb)lb.classList.remove("active")});