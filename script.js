const loader=document.querySelector('.loader');window.addEventListener('load',()=>setTimeout(()=>loader.classList.add('hide'),650));
document.getElementById('year').textContent=new Date().getFullYear();
const menu=document.querySelector('.menu-btn'),nav=document.querySelector('nav');menu.addEventListener('click',()=>nav.classList.toggle('open'));document.querySelectorAll('nav a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));
const words=['NETWORKING','CYBERSECURITY','PYTHON BUILDER','CONTINUOUS LEARNER'];let wi=0,ci=0,deleting=false;const target=document.getElementById('typing');function type(){let w=words[wi];target.textContent=deleting?w.slice(0,--ci):w.slice(0,++ci);let speed=deleting?45:85;if(!deleting&&ci===w.length){speed=1300;deleting=true}else if(deleting&&ci===0){deleting=false;wi=(wi+1)%words.length;speed=250}setTimeout(type,speed)}type();
const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('show')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
const sections=[...document.querySelectorAll('main section[id]')],links=[...document.querySelectorAll('nav a')];window.addEventListener('scroll',()=>{let y=window.scrollY+120;sections.forEach(s=>{if(y>=s.offsetTop&&y<s.offsetTop+s.offsetHeight){links.forEach(a=>a.classList.toggle('active',a.getAttribute('href')==='#'+s.id))}});document.getElementById('backTop').classList.toggle('show',scrollY>700)});document.getElementById('backTop').onclick=()=>scrollTo({top:0,behavior:'smooth'});
const glow=document.querySelector('.cursor-glow');if(matchMedia('(pointer:fine)').matches)window.addEventListener('mousemove',e=>{glow.style.left=e.clientX+'px';glow.style.top=e.clientY+'px'});
document.querySelectorAll('.tilt').forEach(el=>el.addEventListener('mousemove',e=>{let r=el.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;el.style.transform=`perspective(900px) rotateY(${x*5}deg) rotateX(${-y*5}deg)`}));document.querySelectorAll('.tilt').forEach(el=>el.addEventListener('mouseleave',()=>el.style.transform=''));
const c=document.getElementById('particles'),ctx=c.getContext('2d');let pts=[];function resize(){c.width=innerWidth;c.height=innerHeight;let n=innerWidth<700?24:55;pts=Array.from({length:n},()=>({x:Math.random()*c.width,y:Math.random()*c.height,vx:(Math.random()-.5)*.25,vy:(Math.random()-.5)*.25}))}function draw(){ctx.clearRect(0,0,c.width,c.height);ctx.fillStyle='rgba(85,230,255,.5)';for(let i=0;i<pts.length;i++){let p=pts[i];p.x+=p.vx;p.y+=p.vy;if(p.x<0||p.x>c.width)p.vx*=-1;if(p.y<0||p.y>c.height)p.vy*=-1;ctx.beginPath();ctx.arc(p.x,p.y,1,0,7);ctx.fill();for(let j=i+1;j<pts.length;j++){let q=pts[j],d=Math.hypot(p.x-q.x,p.y-q.y);if(d<110){ctx.strokeStyle=`rgba(85,230,255,${.08*(1-d/110)})`;ctx.beginPath();ctx.moveTo(p.x,p.y);ctx.lineTo(q.x,q.y);ctx.stroke()}}}requestAnimationFrame(draw)}resize();addEventListener('resize',resize);draw();